namespace PhoenixLeds
{
    public enum AnimationType
    {
        PlayOnce,
        Loop
    }

    public enum Panel
    {
        Left = 0,
        Down = 1,
        Up = 2,
        Right = 3
    }
}
